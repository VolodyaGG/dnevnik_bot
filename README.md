# 🐾 Pet Survey Bot

Телеграм-бот для ежедневного отслеживания ухода за питомцами. Бот отправляет опросы в заданное время и сохраняет историю ответов.

## ✨ Функциональность

📅 Ежедневные опросы - автоматическая отправка в 19:00 по Москве
📝 Ручной опрос - возможность пройти опрос в любое время
📊 История ответов - просмотр предыдущих ответов
🔔 Гибкая настройка - подписка/отписка от опросов
💾 Локальное хранение - данные сохраняются в JSON файле
🚀 Установка и запуск

### 1. Клонирование и настройка

Клонируйте репозиторий (или скопируйте файлы)
```
git clone <your-repo-url>
cd pet-survey-bot
```

#### Создайте виртуальное окружение (рекомендуется)
```
python -m venv venv
source venv/bin/activate  # Linux/Mac
```
или
```
venv\Scripts\activate  # Windows
```

### 2. Установка зависимостей

```
pip install -r requirements.txt
```
### 3. Настройка токена бота

Создайте файл .env в корневой директории проекта:
```
BOT_TOKEN=your_telegram_bot_token_here
```
### 4. Получение токена бота

Найти @BotFather в Telegram
Отправить команду /newbot
Выбрать имя для бота (например: PetSurveyBot)
Выбрать username для бота (должен заканчиваться на bot, например: pet_survey_bot)
Скопировать полученный токен и вставить в файл .env
### 5. Запуск бота

```
python bot.py
```
#### 📋 Команды бота

/start - Начать работу с ботом, регистрация пользователя
/survey - Пройти опрос вручную
/history - Посмотреть историю своих ответов (последние 10)
/stop - Отписаться от ежедневных опросов

#### 🗃️ Структура данных

Данные пользователей сохраняются в файл user_data.json:

```
json
{
  "123456789": {
    "username": "username",
    "first_name": "Имя",
    "surveys": [
      {
        "date": "2024-01-15 19:00",
        "answers": ["ответ1", "ответ2", "ответ3"]
      }
    ]
  }
}
```

#### 🛠️ Зависимости

Основные зависимости (уже в requirements.txt):
```
aiogram==3.x - асинхронный фреймворк для Telegram Bot API
pytz - работа с временными зонами
python-dotenv - загрузка переменных окружения из .env файла
```

#### ⚙️ Настройка

##### Изменение времени опроса

В файле bot.py измените переменную:

```
SURVEY_TIME = time(19, 00)  # 19:00 по Москве
```
##### Изменение вопросов

В файле bot.py измените список QUESTIONS:

```
QUESTIONS = [
    "Ваш вопрос 1?",
    "Ваш вопрос 2?",
    "Ваш вопрос 3?"
]
```
##### 🐛 Решение проблем

Бот не запускается

Проверьте правильность токена в файле .env
Убедитесь, что все зависимости установлены
Проверьте, что файл .env находится в той же директории, что и bot.py
Опросы не приходят

Убедитесь, что бот запущен и работает

Проверьте временную зону (по умолчанию Москва)

Используйте команду /survey для проверки работы вручную

Ошибки с данными

При проблемах с файлом данных можно удалить user_data.json (бот создаст новый)

##### 📝 Пример использования

Пользователь отправляет /start

Бот приветствует и регистрирует пользователя

Каждый день в 19:00 бот отправляет опрос

Пользователь отвечает на 3 вопроса

Ответы сохраняются в историю

Пользователь может просмотреть историю командой /history

##### 🔧 Технические детали

Хранилище: Локальный JSON файл

Временная зона: Europe/Moscow (Москва)

Интервал опросов: Ежедневно в заданное время

Логирование: В консоль с уровнем INFO🐾 Pet Survey Bot